<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">

<insert id="insert" parameterType="java.util.Map">
 insert into center(cid, actPlace, email, nanmmbyNm, nanmmbyNmAdmn, postAdres, telno, mnnstNm) 
 values((select max(cid)+1 from center), #{actPlace}, #{email}, #{nanmmbyNm}, #{nanmmbyNmAdmn}, #{postAdres}, #{telno}, #{mnnstNm})
</insert> 


<update id="vinfoinsert" parameterType="java.util.Map">
update vinfo
set rcritNmpr = 
<foreach collection="list" item="rcritNmpr" >
 #{rcritNmpr} 
</foreach>, appTotal=
<foreach collection="list" item="appTotal" >
 #{appTotal} 
</foreach>
where progrmRegistNo = (select progrmRegistNo from center where =
<foreach collection="list" item="progrmRegistNo" >
 #{progrmRegistNo} 
</foreach>

</update> 
</mapper>

<!-- 값을 넘길때 # 컬럼 넘길때 $ -->